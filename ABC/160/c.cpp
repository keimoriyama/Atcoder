#include <iostream>
#include <string>
#include <vector>

using namespace std;


int main(int argc, char *argv[])
{
  int K, N;
  cin >> K >> N;
  vector<int> A(N);
  vector<int> dist(N);
  vecrot<bool> visit(N, false);
  for(int i = 0; i < N; i++){
    cin >> A[i];
  }
  for(int i = 0; i < N; i++){

  }
  return 0;
}
