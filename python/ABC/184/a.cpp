#include <iostream>
#include <string>
#include <vector>

using namespace std;


int main(int argc, char *argv[])
{
  int a, b, c, d;

  cin >> a >> b >> c >> d;
  cout << (a * d) - (b * c) << endl;
  return 0;
}
