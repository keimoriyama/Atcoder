#include <bits/stdc++.h>
#include <iostream>
#include <vector>
using namespace std;

#define rep(i, x) for (int i = 0; i < (x); i++)

int main() {
  long long N;
  cin >> N;
  vector A(N, 0);
  rep(i, N) cin >> A[i];
  return 0;
}
